mysql:
  port: "3306"
  config: charset=utf8mb4&parseTime=True&loc=Local
  dbname: my-works-hub
  username: root
  password: "123456"
  path: 127.0.0.1

admin:
  # 管理端固定 token（勿含 & = ? 等 URL 特殊字符；勿与 mysql.config 混用）
  # 首次在 /admin 页填写，或 /admin?token=你的token（token 需 URL 编码）
  token: "workhub-admin-token-change-me-111my-works-hub-187"
  # 管理端登录密码（用于 /admin/login 的密码校验）
  password: "my-works-hub-187"

app:
  portal_origin: "http://localhost:5173"
  html_public_host: ""

